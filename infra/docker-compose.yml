services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: miamatch
      POSTGRES_PASSWORD: miamatch
      POSTGRES_DB: miamatch
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: